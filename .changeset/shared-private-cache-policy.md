---
"@fullsnacklab/astro-security": minor
---

Add configurable response-cache policy and the `/cache` export for private routes, authenticated requests, and responses that set cookies. Applications can opt into private, no-store responses alongside the existing security headers.
