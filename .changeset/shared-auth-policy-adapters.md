---
"@fullsnacklab/astro-better-auth": minor
---

Add reusable authentication policy, route guards, and SQLite and D1 adapters through the `/policy`, `/guard`, `/sqlite`, and `/d1` exports, with `/node` and `/cloudflare` aliases. Applications can share infrastructure while retaining their own route selection and authorization policy.
